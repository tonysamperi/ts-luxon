sudo: true
services:
  - docker
before_install:
  - docker pull tonysamperi/ts-luxon
  - node -v
  - npm install
script:
  - npm run build
  - npm run lint
  - npm run test
  - npm run docs
  - npm run check-doc-coverage
  - bash <(curl -s https://codecov.io/bash)
